# Server
server.port=8080

# Database
spring.datasource.url=jdbc:mysql://${DB_HOST:mainline.proxy.rlwy.net}:${DB_PORT:42307}/${DB_NAME:railway}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:qCxvBmouzIbwTTjAwraIeodOHqcWmBWA}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false

# JWT
jwt.secret=${JWT_SECRET:scholarlink_jwt_super_secret_2024_xYzAbC123}
jwt.expiration=2592000000

# Logging
logging.level.com.scholarlink=INFO
